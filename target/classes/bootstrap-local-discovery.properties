spring.cloud.discovery.enabled=true
spring.cloud.config.discovery.service-id=mssc-brewery-config

# Service fails to start if it cannot connect to cloud for fetching confugration
spring.cloud.config.fail-fast=true

# Credentials to config server to fetch configuration
spring.cloud.config.username=MyUserName
spring.cloud.config.password=MyPassword

# [netflix:eureka] -> username & password for eureka
eureka.client.service-url.defaultZone=http://netflix:eureka@localhost:8761/eureka